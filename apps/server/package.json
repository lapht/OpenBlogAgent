{
  "name": "@openblog/server",
  "version": "0.1.0",
  "private": true,
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@openblog/config": "file:../../packages/config",
    "@openblog/logger": "file:../../packages/logger",
    "@openblog/shared": "file:../../packages/shared",
    "fastify": "^5.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "dev": "tsx src/main.ts",
    "start": "node dist/main.js",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "tsx": "^4.19.0"
  }
}
