# Plugin Model (Foundation)

Third-party extensions should depend on `@openblog/sdk` only.

Design goals:

- Avoid coupling with internal modules
- Versioned public contracts
- Stable interfaces for providers, tools and publishers
