# Development Guide

## Commands

- `npm install`
- `npm run lint`
- `npm run build`
- `npm run test`

## Local API

The server app exposes:

- `GET /health`
- `GET /version`

Run locally with `npm run dev`.
